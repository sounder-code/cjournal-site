name: Weekly K-apt Data Refresh

on:
  schedule:
    # Every Saturday at 05:20 Asia/Seoul (Friday 20:20 UTC).
    - cron: "20 20 * * 5"
  workflow_dispatch:

concurrency:
  group: cjournal-kapt-refresh
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  refresh:
    runs-on: [self-hosted, macOS, ARM64, cjournal]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Preserve current manifest
        run: cp public/data/apartments/manifest.json "${RUNNER_TEMP}/kapt-manifest-before.json"

      - name: Download and transform K-apt bulk files
        run: npm run data:kapt

      - name: Validate refreshed data
        env:
          KAPT_PREVIOUS_MANIFEST: ${{ runner.temp }}/kapt-manifest-before.json
        run: |
          npm run data:kapt:bulk:validate
          npm run data:kapt:refresh:check
          npm run quality:apartments

      - name: Validate site
        run: |
          npm run typecheck
          npm run build

      - name: Commit and push when data changed
        run: |
          if git diff --quiet -- public/data/apartments src/data/apartment-coordinates.json; then
            echo "K-apt data is already current"
            exit 0
          fi
          git config user.name "data-bot"
          git config user.email "data-bot@users.noreply.github.com"
          git add public/data/apartments src/data/apartment-coordinates.json
          git commit -m "chore(data): refresh K-apt dataset"
          git push origin main
